{
  "name": "my-new-project",
  "type": "module",
  "devDependencies": {
    "@faker-js/faker": "^8.3.1",
    "@wdio/allure-reporter": "^8.24.12",
    "@wdio/cli": "^8.26.0",
    "@wdio/local-runner": "^8.26.0",
    "@wdio/mocha-framework": "^8.24.12",
    "@wdio/spec-reporter": "^8.24.12",
    "allure-commandline": "^2.25.0",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "wdio:main": "cross-env ENV=http://localhost:3000/ wdio run ./main.wdio.conf.js && npx allure generate allure-results -o allure-report --clean && npx allure open allure-report || npx allure generate allure-results -o allure-report --clean && npx allure open allure-report"
  }
}
